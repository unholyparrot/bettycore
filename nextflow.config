params {
    // input params
    input = null
    output = null

    // ncbi api token for faster auth
    ncbi_api_token = null

}

profiles {
    conda {
        conda.enabled = true
    }
}
